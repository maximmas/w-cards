const initialState = {
    xxx: []
}

export default function reducerHeader(state = initialState, action) {

    if (action.type === 'next') {
        // nothing to do
    }

    return {
         xxx: []
    }
}