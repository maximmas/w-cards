import React from 'react'
import classes from './Logo.css'

const Logo = (props) => {
    return (
        <div className={classes.Logo}>
            W-CARDS
        </div>
    )
}

export default Logo